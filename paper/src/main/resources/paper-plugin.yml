name: '${name}'
main: '${group}.CrazyEnchantments'
loader: '${group}.loader.CrazyLoader'
bootstrapper: '${group}.loader.CrazyLoader'

version: '${version}'
api-version: '${apiVersion}'
description: '${description}'
website: '${website}'

folia-supported: true

dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true